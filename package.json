{
  "name": "scenix",
  "version": "1.0.0",
  "private": true,
  "description": "AI-driven UI automation testing platform powered by Midscene.js and Appium",
  "scripts": {
    "dev": "concurrently \"pnpm --filter client dev\" \"pnpm --filter server dev\"",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "build": "pnpm --filter client build && pnpm --filter server build",
    "test": "pnpm --filter core exec vitest run --config vitest.config.ts --root .. tests",
    "test:web": "pnpm --filter core exec vitest run --config vitest.config.ts --root .. tests/web/login.test.ts",
    "test:android": "pnpm --filter core exec vitest run --config vitest.config.ts --root .. tests/android/ebay-search.test.ts",
    "test:ios": "pnpm --filter core exec vitest run --config vitest.config.ts --root .. tests/ios/settings.test.ts"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ],
    "peerDependencyRules": {
      "allowAny": [
        "@playwright/test"
      ]
    }
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  }
}
